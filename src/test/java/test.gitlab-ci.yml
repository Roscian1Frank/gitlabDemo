image: maven:latest

stages:
  - test

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

test_job:
  stage: test
  script:
    - mvn test
  only:
    changes:
      - /src/main/java/frontEnd/**/*
      - /src/main/java/backEnd/**/*
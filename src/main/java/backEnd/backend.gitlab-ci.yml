variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

build_backend:
  image: maven:latest
  stage: build
  script:
    - mvn compile
  only:
    changes:
      - /src/main/java/backEnd/**/*